<%= form_for(students_forum) do |f| %>
  <% if students_forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(students_forum.errors.count, "error") %> prohibited this students_forum from being saved:</h2>

      <ul>
      <% students_forum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :主题 %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :描述 %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit "提交"%>
  </div>
<% end %>
